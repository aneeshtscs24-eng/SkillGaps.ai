# SkillGaps.AI – Python Dependencies
# =====================================
# Install with:  pip install -r requirements.txt
#
# Tested on Python 3.10 / 3.11 / 3.12 (CPU-only).
# GPU support: replace torch with torch+cu118 via PyTorch index.
#
# Core ML & NLP
sentence-transformers>=2.7.0   # Hugging Face sentence embeddings (MiniLM etc.)
scikit-learn>=1.4.0            # cosine_similarity, metrics
torch>=2.2.0                   # sentence-transformers backend (CPU build)
transformers>=4.40.0           # Hugging Face model hub (pulled by sentence-transformers)

# Fairness
fairlearn>=0.10.0              # Demographic parity, equalized odds metrics
                               # (imported conceptually; parity computed
                               #  natively in EthicalFairnessLayer for
                               #  zero-dependency flexibility)

# XAI (optional upgrade path)
shap>=0.45.0                   # SHAP explainability (used in extended XAI;
                               #  keyword-overlap XAI works without SHAP)

# Data
pandas>=2.2.0
numpy>=1.26.0

# Utilities
tqdm>=4.66.0                   # Progress bars (used by sentence-transformers)
