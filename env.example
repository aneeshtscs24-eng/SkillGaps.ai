# SkillGaps.AI – Database Credentials Template
# =============================================
# 1. Copy this file:   cp .env.example .env
# 2. Fill in real values below.
# 3. NEVER commit .env to source control — add it to .gitignore.
# 4. Load in Python with: from dotenv import load_dotenv; load_dotenv()

SKILLGAPS_DB_HOST=127.0.0.1
SKILLGAPS_DB_PORT=3306
SKILLGAPS_DB_USER=skillgaps_user
SKILLGAPS_DB_PASS=your_secure_password_here
SKILLGAPS_DB_NAME=skillgaps_ai
